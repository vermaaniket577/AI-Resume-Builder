import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.airesumebuilder.app',
  appName: 'AI Resume Builder',
  webDir: 'dist'
};

export default config;
